WALL FOLLOWING

CHECK IF STALL 
	IF ELAPSED TIME OF STALL SIGNIFICANT
		STALL RECOVERY
STALL RECOVERY
	BACK UP
	CHECK FOR MORE SPACE ON LEFT/RIGHT
	TURN WHERE THERES MORE SPACE

CHECK IF OBJECT IN FRONT
	SLOW DOWN
		TURN RIGHT IF NOT FAR FROM WALL
		
		TURN LEFT IF NOT CLOSE TO WALL AT ALL
	
IF NO OBJECTS IN FRONT
	CHECK IF TOO CLOSE FROM WALL
		TURN RIGHT TO WALL
	CHECK IF TOO FAR FROM WALL
		TURN LEFT TO WALL
	CHECK IF OPTIMAL DISTANCE FROM WALL
		FOLLOW WALL AND MOVE FORWARD
